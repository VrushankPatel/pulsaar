# Rules

- Never skip validation
- Never implement more than one Next step
- Never leave git tree dirty
- Never modify files not required by Next step
- Never change branch
- Never introduce shell or exec functionality into Pulsaar