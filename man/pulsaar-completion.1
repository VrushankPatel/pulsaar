.nh
.TH "PULSAAR" "1" "Jan 2026" "Auto generated by spf13/cobra" ""

.SH NAME
pulsaar-completion - Generate completion script


.SH SYNOPSIS
\fBpulsaar completion [bash|zsh|fish|powershell]\fP


.SH DESCRIPTION
To load completions:

.PP
Bash:

.PP
$ source <(pulsaar completion bash)

.PP
# To load completions for each session, execute once:

.PP
# Linux:

.PP
$ pulsaar completion bash > /etc/bash_completion.d/pulsaar

.PP
# macOS:

.PP
$ pulsaar completion bash > /usr/local/etc/bash_completion.d/pulsaar

.PP
Zsh:

.PP
# If shell completion is not already enabled in your environment,

.PP
# you will need to enable it.  You can execute the following once:

.PP
$ echo "autoload -U compinit; compinit" >> ~/.zshrc

.PP
# To load completions for each session, execute once:

.PP
$ pulsaar completion zsh > "${fpath[1]}/_pulsaar"

.PP
# You will need to start a new shell for this setup to take effect.

.PP
fish:

.PP
$ pulsaar completion fish | source

.PP
# To load completions for each session, execute once:

.PP
$ pulsaar completion fish > ~/.config/fish/completions/pulsaar.fish

.PP
PowerShell:

.PP
PS> pulsaar completion powershell | Out-String | Invoke-Expression

.PP
# To load completions for each session, execute once:

.PP
#    pulsaar completion powershell > pulsaar.ps1

.PP
# and source this file from your PowerShell profile.


.SH OPTIONS
\fB-h\fP, \fB--help\fP[=false]
	help for completion


.SH SEE ALSO
\fBpulsaar(1)\fP


.SH HISTORY
6-Jan-2026 Auto generated by spf13/cobra
